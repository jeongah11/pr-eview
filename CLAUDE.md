# PR 작성 규칙 (PReview 팀 공용)

이 파일은 **Claude Code가 이 저장소에서 Pull Request를 만들 때 따르는 규칙**입니다.
리뷰 경험이 적은 팀원도 변경 내용을 쉽게 이해할 수 있도록, 아래 서식과 원칙을 지켜 주세요.

> 새 프로젝트를 시작하면 이 `CLAUDE.md`를 저장소 루트에 복사해 두면, 팀 전체가 같은 방식으로 PR을 작성하게 됩니다.

## PR 본문 서식

팀원이 "PR 올려줘"라고 하면, PR 본문을 아래 마크다운 형식으로 작성합니다.

```markdown
## 목적
(이 PR이 무엇을 위해 필요한지 한두 문장)

## 주요 변경점
- (바뀐 것들을 항목으로, 파일이 아니라 "무엇이 달라졌는지" 기준)

## 코드 설명 (어떻게 동작하나)
(핵심 로직이 어떤 흐름으로 동작하는지, 리뷰어가 코드를 읽기 전에 그림을 그릴 수 있게)

## 용어 설명
- (어려운 개발 용어·API·패턴이 있으면 한 줄로 풀이 — 없으면 생략)

## 리뷰 포인트 / 고민한 점
- (특히 봐줬으면 하는 부분, 확신이 없어 의견을 듣고 싶은 부분)
```

## PR 생성 절차 (Claude Code용)

`gh pr create`를 실행하기 **전에** 반드시 다음 순서를 지킵니다.

1. 위 서식에 따른 PR 제목·본문 초안을 마크다운으로 작성해 사용자에게 먼저 보여줍니다.
2. 사용자가 내용을 확인하고 수정할 수 있도록 기다립니다.
3. 사용자가 명시적으로 확인("이대로", "올려", "만들어" 등)하면 그때 PR을 생성합니다.

초안을 보여주지 않고 바로 `gh pr create`를 실행해서는 안 됩니다.

## 원칙

- **한 PR에는 하나의 목적만.** 여러 기능이나 관심사가 섞여 있으면, 그 점을 짚어 주고 나눠서 올리기를 권합니다. PR이 작을수록 리뷰하기 쉽습니다.
- **용어는 풀어서.** 리뷰어가 처음 보는 용어가 있으면 "용어 설명"에 짧게 적어, 코드를 이해하는 데 사전 지식이 없어도 되게 합니다.
- **변경점은 파일이 아니라 의미로.** "background.js 수정" 대신 "요청이 30초 넘으면 자동으로 중단되게 함"처럼, 무엇이 어떻게 달라졌는지 적습니다.
